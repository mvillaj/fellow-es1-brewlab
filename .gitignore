node_modules/
dist/
server/data/
*.db
*.db-wal
*.db-shm
.DS_Store
.env
*.log
# Account dumps from the probe/discover/har scripts. Real device ids and
# serials; never commit one.
scripts/fellow-*-output.json
scripts/*.har
# Local deployment notes. Kept out of the repository deliberately: the file is an
# ordered list of what is weakest about a live service, and this repo is public.
DEPLOYING.md
